<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Site-PostgreSQL</title>
  <link rel="stylesheet" href="../scss/global.css">
  <link rel="stylesheet" href="../scss/playbill.css">
</head>

<body>
  <header class="header">
    <div class="header__exit">
      <a href="/playbills">
        <svg role="presentation" class="t-popup__close-icon" width="15" height="20" viewBox="0 0 11 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L10 10L1 19" stroke="#000000" stroke-width="2"></path></svg>
      </a>
    </div>
  </header>
  <main class="content">
    <div class="content__container">
      <% playbillItems.forEach(function(playbillItem) { %>
        <div class="playbill__block">
          <div class="playbill__title">
            <h2>
              <%= playbillItem.name %>
            </h2>
          </div>
          <div class="playbill__img">
            <img src="<%= playbillItem.photoafisha %>" alt="<%= playbillItem.photoafisha %>">
          </div>
          <div class="playbill__description">
            <span>
              <%= playbillItem.description %>
            </span>
          </div>
          <div class="playbill__date">
            <span>
              <%= playbillItem.date %>
            </span>
          </div>
          <div class="playbill__location">
            <span>
              <%= playbillItem.location %>
            </span>
          </div>
          <div class="playbill__group">
            <span>
              Группа: <%= playbillItem.group %>
            </span>
          </div>
          <div class="playbill__teachers">
            <span>
              <% teachers.forEach(function(teacher) { %> 
                Преподаватель: <%= teacher.surname %>
              <% }); %>
            </span>
          </div>
        </div>
        <% }); %>
    </div>
  </main>
</body>

</html>